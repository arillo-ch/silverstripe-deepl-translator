{
  "name": "silverstripe-deepl",
  "version": "1.0.0",
  "description": "Deepl translations for SilverStripe",
  "repository": {
    "type": "git",
    "url": "git@github.com:arillo-ch/silverstripe-deepl-translator.git"
  },
  "scripts": {
    "dev": "esbuild client/js/glossaryEditor.js --sourcemap --bundle --outdir=client/dist --watch",
    "build": "esbuild client/js/glossaryEditor.js --bundle --minify --outdir=client/dist"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.15.8"
  },
  "dependencies": {}
}